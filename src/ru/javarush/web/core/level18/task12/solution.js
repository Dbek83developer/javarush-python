// Создание объекта user с двумя свойствами: name и age
let user = {
  name: 'John Doe',
  age: 30
};

// Изменение значения свойства age на новое значение
user.age = 35;

// Добавление нового свойства email и присвоение ему значения
user.email = 'john.doe@example.com';

// Вывод всех свойств объекта user в консоль
console.log(user);