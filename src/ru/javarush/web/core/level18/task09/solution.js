// Объявление глобальной переменной
var globalMessage = "Hello from global scope";

// Создание функции
function showMessage() {
    // Вывод значения глобальной переменной в консоль
    console.log(globalMessage);
}

// Вызов функции
showMessage();