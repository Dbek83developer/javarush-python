// Объявление переменной user и инициализация объектом с полем name
let user = {
  name: "Alice"
};

// Вывод значения переменной user до изменения
console.log(user);

// Присвоение переменной user значения null
user = null;

// Вывод значения переменной user после изменения
console.log(user);