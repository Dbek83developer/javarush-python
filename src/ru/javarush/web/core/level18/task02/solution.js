// Объявление переменной num, содержащей число 200
let num = 200;

// Объявление переменной str, содержащей строку "300"
let str = "300";

// Сложение переменных num и str, результат присваиваем переменной result
let result = num + str;

// Вывод значения переменной result в консоль
console.log(result);

// Вывод типа переменной result в консоль
console.log(typeof(result));