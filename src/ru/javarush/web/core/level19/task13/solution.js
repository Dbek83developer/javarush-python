// Создание исходного массива фруктов
const fruits = ['apple', 'banana', 'cherry', 'date', 'fig'];

// Копирование части массива с индексами от 1 до 3 включительно
const subset = fruits.slice(1, 4);

// Вывод нового массива
console.log(subset);